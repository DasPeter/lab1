name: lab1
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.1 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  avatar_glow: ^2.0.2
  flutter_launcher_icons: "^0.10.0"
  font_awesome_flutter:
  provider: ^6.0.3
  http: ^0.13.5
  record: ^4.4.0
  path_provider: ^2.0.11
  url_launcher: ^6.1.6
  

flutter_icons:
  image_path_android: "assets/images/waves5.jpg"
  android: true
  # (flutter_launcher_icons) To update app icons use:
  # flutter pub get
  # flutter pub run flutter_launcher_icons:main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/images/